<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rosantos.servicealarm">

    <uses-permission android:name='android.permission.WAKE_LOCK'/>

    <application
        android:name=".ServiceAlarmApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity_"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.MyIntentService_"
            android:exported="false"></service>

        <receiver android:name=".service.AlarmBroadcastReceiver">
            <intent-filter>
                <action android:name="com.example.rosantos.servicealarm.service.action.ACTION_ALARM" />
            </intent-filter>
        </receiver>
    </application>

</manifest>